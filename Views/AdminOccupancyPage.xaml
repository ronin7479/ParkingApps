<Page x:Class="ParkingApp.Views.AdminOccupancyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#1A1A1A">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="24,16,24,0">
            <TextBlock Text="Заповненість паркінгів"
                       FontSize="22" FontWeight="Bold"
                       Foreground="White"/>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="16,12,16,16">
            <WrapPanel x:Name="OccupancyPanel"
                       Orientation="Horizontal"
                       HorizontalAlignment="Left"/>
        </ScrollViewer>
    </Grid>
</Page>
